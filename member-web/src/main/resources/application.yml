spring:
  profiles:
    active: @profile.active@
  application:
    name: ${project.name}
  output:
    ansi:
      enabled: always
  mvc:
    static-path-pattern: /**

server:
  port: 8080

logging:
  level:
    - root: info

member:
  service:
    version: 1.0.0
nacos:
  host: 192.168.6.106
  port: 8848

dubbo:
  registry:
    address: nacos://${nacos.host}:${nacos.port}
  consumer:
    version: ${member.service.version}

workerId: 1